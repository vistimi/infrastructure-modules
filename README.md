# infrastructure

## run

Open the project with the dev container.

```shell
terraform -chdir=scraper init

terraform -chdir=scraper apply
```

## env

#### devcontainer

```
AWS_REGION=***
AWS_PROFILE=***
AWS_ACCESS_KEY=***
AWS_SECRET_KEY=***
```